
<?php

function SomaSimples($a, $b) {
    // nome(parâmetro formal), tipo, parâmetros e tipo de retorno (em PHP não é necessário declarar o tipo de retorno explicitamente)
    echo "O resultado da soma é: " . ($a + $b);
}

// chamada da função
SomaSimples(3, 5); // chamada da função

?>
