// Vetores (arrays) e Matrizes
// ARRAY/VETOR/MATRIZ : mesma coisa, uma grande variável (aramazenamento de memória) que aramazena mais de uma variável

#include <stdio.h>
#include <stdlib.h>

int main () {

    int la_numeros[] = {1,4,3,7,100,50};

    printf("\nla_numeros %d", la_numeros[0]);
    printf("\nla_numeros %d", la_numeros[1]);
    printf("\nla_numeros %d", la_numeros[2]);
    printf("\nla_numeros %d", la_numeros[3]);
    printf("\nla_numeros %d", la_numeros[4]);
    printf("\nla_numeros %d", la_numeros[5]);

    return 0;
}